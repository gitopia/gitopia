syntax = "proto3";
package gitopia.gitopia.rewards;

option go_package = "github.com/gitopia/gitopia/x/rewards/types";

message Rewards {
  string recipient = 1; 
  // map from creator to reward
  map<string, Reward> rewardsByCreator = 2; 
}

message Reward {
  string totalAmount = 1;
  string claimedAmount = 2;
}